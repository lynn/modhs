module Constants where

initialTicksPerRow :: Int
initialTicksPerRow = 6

initialTempo :: Int
initialTempo = 125

rowsPerPattern :: Int
rowsPerPattern = 64

outputBitRate :: Double
outputBitRate = 44100.0 -- Hz

palClockRate :: Double
palClockRate = 7093789.2 -- Hz

ntscClockRate :: Double
ntscClockRate = 7159090.5 -- Hz